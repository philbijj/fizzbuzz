var count, fizzer, buzzer, fizzbuzzer;
t = function (start, end)

count = function(start, end)
	var output = start;
	for (var counter = start; counter < end; counter +=1) {
	output = output + " " + (counter + 1) + buzzer(i + 1);
	}
return output;
};

fizzer = function(num {
};

buzzer =   {
};

}
};